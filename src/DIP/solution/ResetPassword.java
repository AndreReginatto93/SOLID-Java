package DIP.solution;

public class ResetPassword {
    private DBConnection dbConnection;

    ResetPassword(DBConnection dbConnection){
        this.dbConnection = dbConnection;

        //...
    }
}
